// Command play is an unofficial, agent-friendly CLI for PLAY Airlines.
package main

import (
	"fmt"
	"os"
)

var version = "dev"

func main() {
	if len(os.Args) < 2 {
		usage()
		os.Exit(2)
	}
	var err error
	switch os.Args[1] {
	case "search":
		err = cmdSearch(os.Args[2:])
	case "read":
		err = cmdRead(os.Args[2:])
	case "session":
		err = cmdSession(os.Args[2:])
	case "brands":
		cmdBrands()
	case "version", "--version", "-v":
		fmt.Println(version)
	case "help", "-h", "--help":
		usage()
	default:
		fmt.Fprintf(os.Stderr, "unknown command %q\n\n", os.Args[1])
		usage()
		os.Exit(2)
	}
	if err != nil {
		fmt.Fprintln(os.Stderr, "error:", err)
		os.Exit(1)
	}
}

func usage() {
	fmt.Fprintf(os.Stderr, `play — unofficial CLI for PLAY Airlines

USAGE:
  play search [--json] --from ORIGIN --to DEST --depart DATE [--return DATE]
  play read [--json] <id|url>
  play session chrome [--wait] [--timeout 3m]
  play session sync
  play session doctor [--json]
  play brands
  play version | help
`)
}
